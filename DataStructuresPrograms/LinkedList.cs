using System;
using System.Collections.Generic;
using System.Text;

namespace DataStructuresPrograms
{
    class LinkedList
    {

        public static void LinkedListTest(LinkedList<string> InputLL)
        {

            //MyList.AddAfter(MyList.First, "Swati");

            //LinkedList<string> RevList = new LinkedList<string>();

            //if (!InputLL.Contains(InputLL.First.Next.ToString()))
            //{
            //    return RevList.AddFirst(InputLL.First);
            //}

            //return RevList;
            //foreach (string str in RevList)
            //{
            //    Console.WriteLine("My reversed list has : " + str);

            //}
            //Console.ReadLine();
            //LinkedList<string> revList = new LinkedList<string>();
            //var count = InputLL.Count;
            //var current = InputLL.First;
            //for(int i = 0; i < count; i++)
            //{
            //    revList.AddFirst(current.Value);
            //    current = current.Next;
            //}

            //Console.WriteLine("My reversed list has:");

            //foreach(string str in revList)
            //{
            //    Console.WriteLine(" " + str);
            //}
            //Console.ReadLine();
  
        }
    }
}
